
import React from 'react';

// This component is deprecated and no longer used.
// The application now relies on the native OS touch keyboard.
const VirtualKeyboard: React.FC = () => {
  return null;
};

export default VirtualKeyboard;
